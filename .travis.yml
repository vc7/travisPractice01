language: objective-c
matrix:
  include:
    - osx_image: xcode8.2
env:
  global:
    - LC_CTYPE=en_US.UTF-8
script:
  - xctool -scheme travisPractice01 -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO
  - xcodebuild -scheme travisPractice01 -destination 'platform=iOS Simulator,name=iPhone 6' test
